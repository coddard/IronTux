# IronTux
IronTux is an enterprise-grade Python utility for securing VPS and homelab environments. It automates SSH hardening, Fail2Ban setup, kernel protections, and Docker-safe firewall configurations without relying on external web requests.
